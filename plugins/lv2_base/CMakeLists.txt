IF(LMMS_HAVE_LILV)
	INCLUDE(BuildPlugin)
	INCLUDE_DIRECTORIES(${LILV_INCLUDE_DIRS})
	INCLUDE_DIRECTORIES(.)
	LINK_LIBRARIES(${LILV_LIBRARY})
	BUILD_PLUGIN(lv2base lv2_base.cpp Lv2Plugin.cpp)
ENDIF(LMMS_HAVE_LILV)
