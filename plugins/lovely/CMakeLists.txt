IF(LMMS_HAVE_LILV)
	INCLUDE(BuildPlugin)
	INCLUDE_DIRECTORIES("${CMAKE_CURRENT_SOURCE_DIR}/../lv2_base")
	INCLUDE_DIRECTORIES(${LILV_INCLUDE_DIRS})
	INCLUDE_DIRECTORIES(.)
	LINK_DIRECTORIES("${CMAKE_CURRENT_BINARY_DIR}/../lv2_base")
	LINK_LIBRARIES(${LILV_LIBRARIES})
	LINK_LIBRARIES(lv2base)
	BUILD_PLUGIN(lovely Lovely.cpp Lovely.h LovelySubPluginFeatures.cpp LovelySubPluginFeatures.h MOCFILES Lovely.h EMBEDDED_RESOURCES "${CMAKE_CURRENT_SOURCE_DIR}/*.png")
ENDIF(LMMS_HAVE_LILV)

